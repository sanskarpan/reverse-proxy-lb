apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rplb.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rplb.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
